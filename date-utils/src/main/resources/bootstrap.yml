spring:
  application:
    name: date-utils
  profiles:
    active: instance-a
  cloud:
    config:
      uri: http://localhost:8888
management:
  endpoints:
    web:
      exposure:
        include: "*"
#  This is not needed as ribbon is deprecated and not included
#  https://stackoverflow.com/questions/65469735/versions-spring-boot-spring-cloud-ribbon-not-working
#    loadbalancer:
#      ribbon:
#        enabled: false