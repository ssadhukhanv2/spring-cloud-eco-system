spring:
  application:
    name: date-utils
  profiles:
    active: instance-a
  cloud:
    config:
      uri: http://localhost:8888
management:
  endpoints:
    web:
      exposure:
        include: "*"