spring:
  application:
    name: config-server
  profiles:
    include:
      git, native #vault Vault Configuration Removed, need to check later
  cloud:
    config:
      server:
#        vault configuration is not working so commenting the code for now. Will check later
#        vault:
#          port: 8200
#          host: 127.0.0.1
#          kvVersion: 2
#          order: 3
        git:
          uri: https://github.com/ssadhukhanv2/config-repo.git
          searchPaths: spring-cloud-eco-system
          order: 2
        native:
          search-locations: classpath:/config
          order: 1
    
server:
  port: 8888
  
encrypt:
  key: fje83Ki8403Iod87dne7Yjsl3THueh48jfuO9j4U2hf64Lo 


#Enable all Spring Boot Actuator endpoints.
management:
  endpoints:
    web:
      exposure:
        include: "*"